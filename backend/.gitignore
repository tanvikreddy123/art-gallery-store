# Dependencies and local packages
# Never commit the node_modules folder, it's huge and can be re-installed with `npm install`
/node_modules

# Environment Variables
# This is the most important line for security.
# It prevents your database URI and JWT secret from being pushed to GitHub.
.env

# Logs
# Log files can contain sensitive information and grow very large.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS-specific files
# These are junk files created by operating systems.
.DS_Store
Thumbs.db