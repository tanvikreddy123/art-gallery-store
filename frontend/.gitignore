# Dependencies
# Never commit the node_modules folder.
/node_modules

# Build artifacts
# The /dist folder is generated when you build the project for production.
# It should be generated on the server, not stored in Git.
/dist

# Vite-specific cache
# Vite creates a cache directory that should not be versioned.
.vite/

# Logs
# Log files can contain sensitive information and grow very large.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS-specific files
# These are junk files created by operating systems.
.DS_Store
Thumbs.db

# Environment variables
# While we are not using a .env file on the frontend in this project,
# it is standard practice to ignore them.
.env.local
.env.development.local
.env.test.local
.env.production.local